const Alien = require("../scripts/alien.js")

document.addEventListener("DOMContentLoaded", function () {
    const canvasEl = documentEl.querySelector('#alien-canvas');
    const ctx = canvasEl.getContext('2d');
});